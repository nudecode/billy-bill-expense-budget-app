
// Alpine.js is managed by Livewire 4 — do not import or start it here.
// Livewire bundles Alpine internally; a second import causes
// "multiple instances of Alpine" which breaks all wire:click events.
